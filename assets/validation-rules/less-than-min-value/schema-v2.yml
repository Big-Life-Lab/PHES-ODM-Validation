---
sites:
  type: list
  schema:
    type: dict
    schema:
      geoLat:
        minInclusive: true
        meta:
        - ruleId: less_than_min_value
          meta:
          - partID: geoLat
            minValue: "-90"
  meta:
    - partID: sites
      partType: tables

```{python}
#| echo: false
import os
import sys
sys.path.append("../src")

from odm_validation.doc_utils import (
    get_rule_asset_dir,
    pprint_csv_file,
    pprint_dict_list,
    pprint_json_file,
    pprint_yaml_file,
)


def asset(filename):
    """Returns the path to `ASSET_DIR`/`filename`."""
    # this function must be defined in each Quarto file to access `ASSET_DIR`
    return os.path.join(ASSET_DIR, filename)
```
